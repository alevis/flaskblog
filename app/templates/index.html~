<!DOCTYPE html>
<html>
<head>
{% if title %}
	<title>{{ title }} - levis</title><
{% else %}
	<title>Weclome to levis page.</title>
{% endif %}
</head>
	<body>
		<h1>Hello, {{ user.nickname }}!</h1>
		{% for post in posts %}
		<div>
			<p>
			{{ post.author.nickname }} says: <b>{{ post.body }}</b>
			</p>
		</div>
	</body>
</html>
